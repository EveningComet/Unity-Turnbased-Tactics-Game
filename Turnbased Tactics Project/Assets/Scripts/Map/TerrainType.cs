using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace TurnbasedGame.Tiles
{
    /// <summary>
    /// A component on the <see cref="GameTile"/> object that will change a <see cref="Units.Unit"/>'s stats
    /// while that unit is on the tile.
    /// </summary>
    public struct TerrainType
    {
        // Defense change
        // Evasion change
    }
}
